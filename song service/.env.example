# song service — copy to .env and fill in real values
PORT=8000

# Neon Postgres connection string (albums + songs)
DB_URL=postgresql://<user>:<password>@<host>.neon.tech/<db>?sslmode=require

# Redis cache password (reads are cached here)
Redis_Password=<redis-password>
