# user service — copy to .env and fill in real values
PORT=5000

# MongoDB Atlas connection string (stores user accounts)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=<app>

# Secret used to sign/verify JWTs. Use a long random string.
JWT_SEC=<random-secret>
